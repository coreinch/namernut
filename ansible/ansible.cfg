[defaults]
inventory = inventory/hosts.ini
retry_files_enabled = False

[privilege_escalation]
# The inventory connects as root directly, so there's no separate user
# to escalate from.
become = False
